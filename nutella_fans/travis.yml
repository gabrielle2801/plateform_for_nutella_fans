language: python
python:
  - '3.5'

before_script:
  - pip install -r requirements.txt

env: DJANGO_SETTINGS_MODULE="nutella_fans.settings.test"

services:
  - postgresql

script:
  - ./manage.py test
